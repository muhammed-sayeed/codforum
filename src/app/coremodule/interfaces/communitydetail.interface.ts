import { singleCommunity } from "./singleCommunity.interface"

export interface communityDetails{
    community:singleCommunity
    inVisibleArt:[]
    isMember:boolean
    questions:[]
    userType:boolean
    visibleArt:[]
}